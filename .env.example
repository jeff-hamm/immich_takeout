# Example environment configuration
# Copy this to .env and fill in your actual values

# KasmWeb Chrome version for login-helper container
kasmweb_version=1.18.0
GOOGLE_PASSWORD=<your password>

# Immich server URL (no trailing slash)
IMMICH_SERVER=http://YOUR_SERVER_IP:2283

# Server IP for VNC and automation
SERVER_IP=192.168.1.216

# VNC password for login-helper
VNC_PASSWORD=password

# Path to Google Drive sync location
GDRIVE_PATH=/mnt/user/jumpdrive/gdrive

# Path to imports directory (metadata, extracted files)
IMPORTS_PATH=/mnt/user/jumpdrive/imports

# Path to rclone config file
RCLONE_CONFIG=/mnt/user/appdata/rclone/rclone.conf

# Path to state directory (API keys, album state)
STATE_PATH=./state

# Rclone remote name (with colon)
RCLONE_REMOTE=gdrive:

# Override paths for specific services (optional)
# RCLONE_TAKEOUT_PATH=gdrive:Takeout
# RCLONE_GDRIVE_PATH=gdrive: